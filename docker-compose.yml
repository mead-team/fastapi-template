version: '3.9'

services:
  app:
    container_name: app-api
    build: .
    command: bash -c "uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
    volumes:
      - ./:/usr/src/app
    ports:
      - 8000:8000
    env_file:
      - env/base.env
    networks:
      - template-network
